listenaddr = ":8000"

[proxy]
upstreamURL = "http://localhost:9000/"
maxIdleConn = 100
maxIdleConnPerHost = 100
idleConnTimeout = "10s" # Note: in form of Go duration string (e.g., "10s", "5m", "1h") 
